language: python
sudo: false
install:
    - pip install -r requirements.txt
    - pip install flake8
script: flake8 setup.py dwgstrings
notifications:
    email:
        recipients:
            - divergentdave@gmail.com
        on_success: change
        on_failure: change
